server.port=${PORT:8080}
spring.application.name=hospital-order-management

# Optional: comma-separated extra CORS origin patterns for a deployed static frontend (e.g. https://*.onrender.com)
# app.cors.extra-origin-patterns=https://your-frontend.onrender.com
