server.port=${PORT:8080}
spring.application.name=hospital-order-management

# Comma-separated CORS origin patterns for static frontends (Render static, GitHub Pages, etc.).
# Example: https://*.onrender.com,https://*.github.io
app.cors.extra-origin-patterns=${CORS_EXTRA_PATTERNS:https://*.onrender.com,https://*.github.io}

